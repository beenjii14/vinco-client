export interface ILogin {
    email: string;
    password: string;
}

export interface IUser {
    active: boolean;
    email: string;
    firstName: string;
    id: string;
    lastName: string;
    name: string;
    role: string;
}
